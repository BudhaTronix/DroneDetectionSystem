# Drone Webcam Detector

Detect drones in real time from a webcam using a pretrained YOLO model.

## Conda Setup

Create and activate the dedicated Conda environment named `dds`:

```bash
conda create -n dds python=3.10 -y
conda activate dds
pip install -r requirements.txt
```
